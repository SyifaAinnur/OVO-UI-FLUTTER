import 'package:flutter/material.dart';
import 'package:flutter_svg/flutter_svg.dart';

class MetodeLain extends StatelessWidget {
  const MetodeLain({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
    );
  }
}